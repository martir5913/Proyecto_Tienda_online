$kind: http-request
url: "{{base_url}}/api/wishlist.php?action=add"
method: POST
headers:
  Accept: application/json
  Content-Type: application/json
queryParams:
  action: add
body:
  type: text
  content: |-
    {
      "id_producto": {{product_id}}
    }
order: 4000
