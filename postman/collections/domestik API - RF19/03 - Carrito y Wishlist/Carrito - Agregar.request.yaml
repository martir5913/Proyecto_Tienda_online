$kind: http-request
url: "{{base_url}}/api/carrito.php?action=add"
method: POST
headers:
  Accept: application/json
  Content-Type: application/json
queryParams:
  action: add
body:
  type: text
  content: |-
    {
      "id_producto": {{product_id}},
      "cantidad": 1
    }
order: 2000
